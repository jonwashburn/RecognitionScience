import Mathlib

namespace Manifest
namespace Generators
/-- Route B manifest: Generators ⇒ Bridge ⇒ Consequences (closure) — demo text. -/
@[simp] def routeB : String := "Route B: Generators ⇒ Bridge ⇒ C — OK (demo)."
end Generators
end Manifest
